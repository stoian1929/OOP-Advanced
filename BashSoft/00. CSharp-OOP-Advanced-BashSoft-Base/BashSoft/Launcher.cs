using BashSoft.Executor;
using BashSoft.Executor.Contracts;
using BashSoft.IO;
using BashSoft.IO.Executor;
using BashSoft.Judge;
using BashSoft.Judge.Contracts;
using BashSoft.Repository;
using BashSoft.Repository.Contracts;
using System;

namespace BashSoft
{
    public class Launcher
    {
        public static void Main()
        {
            

            //IContentComparer tester = new Tester();
            //IDirectoryManager ioManager = new IOManager();
            //IDatabase repo = new StudentsRepository(new RepositoryFilter(), new RepositorySorter());

            //ICommandInterpreter currentInterpreter = new CommandInterpreter(tester, repo, ioManager);
            //IReader reader =  new InputReader(currentInterpreter);

            //reader.StartReadingCommands();
        }
    }
}
